[package]
name = "mouse-testkit"
version = "0.1.0"
edition = "2021"
description = "A standalone mouse testing utility"
authors = ["Mouse-TestKit Contributors"]

[dependencies]
crossterm = "0.27"
evdev = "0.12"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
